;
; Multiply two numbers using a loop (Ben Eater's design)
;

.org $4000

loop:	
	lda prod
	adc x
	sta prod
	lda y
	sub one
	beq end
	sta y
	jmp loop

end:    
	lda prod
	sta 0
	hlt

one: 	 
	.db 1
x:
	.db 15
y:
	.db 17 	
prod:	 
	0